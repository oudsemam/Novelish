export interface Book {
    title: string,
    author: string,
    genre: string,
    isbn: number,
    progress: number,
    cover: string,
    want: boolean,
    read: boolean,
    dnf: boolean,
    burn: boolean,
    current: boolean
}
